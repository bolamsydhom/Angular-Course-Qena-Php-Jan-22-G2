import { CustomeDirectveDirective } from './custome-directve.directive';

describe('CustomeDirectveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomeDirectveDirective();
    expect(directive).toBeTruthy();
  });
});
